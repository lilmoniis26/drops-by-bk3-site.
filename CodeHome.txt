
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Drops By BK3 – Tactical HUD</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <style>
    :root {
      --bg: #050608;
      --bg-alt: #0b0f14;
      --neon: #35ff7a;
      --neon-soft: rgba(53, 255, 122, 0.2);
      --text-main: #f5f5f5;
      --text-muted: #9ea4b0;
      --danger: #ff3b4f;
      --accent: #35c7ff;
      --border-soft: #1a222d;
      --radius: 8px;
      --transition-fast: 0.2s ease-out;
      --transition-med: 0.3s ease-out;
      --shadow-neon: 0 0 18px rgba(53, 255, 122, 0.45);
    }

    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    body {
      font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
      background: radial-gradient(circle at top, #101622 0, #050608 55%, #020308 100%);
      color: var(--text-main);
      min-height: 100vh;
      display: flex;
      flex-direction: column;
    }

    a {
      color: inherit;
      text-decoration: none;
    }

    /* ====== GLOBAL HUD WRAPPER ====== */

    .hud-shell {
      display: flex;
      min-height: 100vh;
      position: relative;
      overflow-x: hidden;
    }

    /* ====== SLIDE-IN MENU TRIGGER ====== */

    .hud-menu-toggle {
      position: fixed;
      top: 16px;
      left: 16px;
      z-index: 1001;
      width: 46px;
      height: 46px;
      border-radius: 50%;
      border: 1px solid var(--neon);
      background: radial-gradient(circle at 30% 30%, #1bff7a33, #050608 70%);
      display: flex;
      align-items: center;
      justify-content: center;
      box-shadow: var(--shadow-neon);
      cursor: pointer;
      transition: transform var(--transition-fast), box-shadow var(--transition-fast), border-color var(--transition-fast);
    }

    .hud-menu-toggle:hover {
      transform: scale(1.05);
      box-shadow: 0 0 26px rgba(53, 255, 122, 0.7);
      border-color: #7bffc0;
    }

    .hud-menu-toggle-icon {
      width: 30px;
      height: 30px;
      border-radius: 50%;
      border: 2px solid var(--neon);
      position: relative;
      display: flex;
      align-items: center;
      justify-content: center;
      overflow: hidden;
    }

    /* Simple “skull-like” icon using shapes (placeholder for your real SVG later) */
    .hud-menu-toggle-icon::before,
    .hud-menu-toggle-icon::after {
      content: "";
      position: absolute;
      border-radius: 50%;
      border: 2px solid var(--neon);
    }

    .hud-menu-toggle-icon::before {
      width: 16px;
      height: 16px;
      top: 6px;
    }

    .hud-menu-toggle-icon::after {
      width: 10px;
      height: 10px;
      bottom: 4px;
      border-top: none;
    }

    .hud-menu-toggle-eyes {
      position: absolute;
      top: 11px;
      width: 14px;
      display: flex;
      justify-content: space-between;
    }

    .hud-menu-toggle-eyes span {
      width: 3px;
      height: 3px;
      border-radius: 50%;
      background: var(--neon);
      box-shadow: 0 0 8px var(--neon);
    }

    /* ====== SLIDE-IN MENU PANEL ====== */

    .hud-menu-backdrop {
      position: fixed;
      inset: 0;
      background: radial-gradient(circle at top left, rgba(53, 255, 122, 0.18), rgba(0, 0, 0, 0.8));
      opacity: 0;
      pointer-events: none;
      transition: opacity var(--transition-med);
      z-index: 1000;
    }

    .hud-menu {
      position: fixed;
      top: 0;
      left: 0;
      width: 290px;
      max-width: 80vw;
      height: 100vh;
      background: linear-gradient(145deg, #050608 0, #050608 40%, #0b1118 100%);
      border-right: 1px solid var(--neon-soft);
      box-shadow: 0 0 40px rgba(0, 0, 0, 0.9);
      transform: translateX(-100%);
      transition: transform var(--transition-med);
      z-index: 1002;
      display: flex;
      flex-direction: column;
      padding: 18px 18px 24px;
    }

    .hud-menu-header {
      display: flex;
      align-items: center;
      gap: 12px;
      margin-bottom: 18px;
      padding-bottom: 12px;
      border-bottom: 1px solid var(--border-soft);
    }

    .hud-menu-logo {
      width: 46px;
      height: 46px;
      border-radius: 50%;
      border: 2px solid var(--neon);
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 11px;
      text-align: center;
      text-transform: uppercase;
      letter-spacing: 0.08em;
      color: var(--neon);
      box-shadow: var(--shadow-neon);
    }

    .hud-menu-title {
      display: flex;
      flex-direction: column;
      gap: 2px;
    }

    .hud-menu-title-main {
      font-size: 14px;
      letter-spacing: 0.16em;
      text-transform: uppercase;
      color: var(--text-main);
    }

    .hud-menu-title-sub {
      font-size: 11px;
      text-transform: uppercase;
      color: var(--text-muted);
      letter-spacing: 0.18em;
    }

    .hud-menu-section-label {
      font-size: 10px;
      text-transform: uppercase;
      letter-spacing: 0.22em;
      color: var(--text-muted);
      margin: 14px 0 6px;
    }

    .hud-menu-nav {
      display: flex;
      flex-direction: column;
      gap: 6px;
    }

    .hud-menu-link {
      display: flex;
      align-items: center;
      justify-content: space-between;
      padding: 9px 10px;
      border-radius: 6px;
      border: 1px solid transparent;
      background: transparent;
      color: var(--text-main);
      font-size: 12px;
      text-transform: uppercase;
      letter-spacing: 0.16em;
      cursor: pointer;
      transition: background var(--transition-fast), border-color var(--transition-fast), transform var(--transition-fast), box-shadow var(--transition-fast);
    }

    .hud-menu-link span {
      opacity: 0.9;
    }

    .hud-menu-link-indicator {
      width: 8px;
      height: 8px;
      border-radius: 50%;
      border: 1px solid var(--neon-soft);
      box-shadow: 0 0 6px rgba(53, 255, 122, 0.4);
    }

    .hud-menu-link:hover {
      background: radial-gradient(circle at left, rgba(53, 255, 122, 0.18), transparent 70%);
      border-color: var(--neon-soft);
      transform: translateX(2px);
      box-shadow: 0 0 14px rgba(53, 255, 122, 0.4);
    }

    .hud-menu-link.active {
      border-color: var(--neon);
      background: linear-gradient(90deg, rgba(53, 255, 122, 0.18), transparent);
      box-shadow: var(--shadow-neon);
    }

    .hud-menu-footer {
      margin-top: auto;
      padding-top: 12px;
      border-top: 1px solid var(--border-soft);
      font-size: 10px;
      color: var(--text-muted);
      display: flex;
      flex-direction: column;
      gap: 4px;
    }

    .hud-menu-footer span {
      letter-spacing: 0.16em;
      text-transform: uppercase;
    }

    /* ====== MENU OPEN STATE (controlled by JS class) ====== */

    .hud-shell.menu-open .hud-menu {
      transform: translateX(0);
    }

    .hud-shell.menu-open .hud-menu-backdrop {
      opacity: 1;
      pointer-events: auto;
    }

    /* ====== MAIN CONTENT AREA ====== */

    .hud-main {
      flex: 1;
      padding: 72px 18px 24px;
      width: 100%;
      max-width: 1200px;
      margin: 0 auto;
      position: relative;
    }

    .hud-main-grid {
      display: grid;
      grid-template-columns: minmax(0, 2.1fr) minmax(0, 1.4fr);
      gap: 18px;
    }

    @media (max-width: 900px) {
      .hud-main-grid {
        grid-template-columns: minmax(0, 1fr);
      }
    }

    /* ====== HUD CARDS ====== */

    .hud-card {
      background: radial-gradient(circle at top left, rgba(53, 255, 122, 0.12), #050608 55%);
      border-radius: var(--radius);
      border: 1px solid var(--border-soft);
      padding: 14px 14px 16px;
      position: relative;
      overflow: hidden;
    }

    .hud-card::before {
      content: "";
      position: absolute;
      inset: 0;
      background-image: linear-gradient(
        120deg,
        rgba(53, 255, 122, 0.08) 0,
        transparent 40%,
        transparent 60%,
        rgba(53, 255, 122, 0.08) 100%
      );
      mix-blend-mode: soft-light;
      opacity: 0.4;
      pointer-events: none;
    }

    .hud-card-header {
      display: flex;
      align-items: center;
      justify-content: space-between;
      margin-bottom: 10px;
    }

    .hud-card-title {
      font-size: 11px;
      text-transform: uppercase;
      letter-spacing: 0.22em;
      color: var(--text-muted);
    }

    .hud-card-tag {
      font-size: 10px;
      text-transform: uppercase;
      letter-spacing: 0.18em;
      padding: 3px 8px;
      border-radius: 999px;
      border: 1px solid var(--neon-soft);
      color: var(--neon);
      background: rgba(53, 255, 122, 0.08);
    }

    .hud-card-body {
      position: relative;
      z-index: 1;
    }

    .hud-heading-main {
      font-size: 20px;
      letter-spacing: 0.08em;
      text-transform: uppercase;
      margin-bottom: 6px;
    }

    .hud-heading-sub {
      font-size: 12px;
      color: var(--text-muted);
      max-width: 420px;
    }

    .hud-pill-row {
      display: flex;
      flex-wrap: wrap;
      gap: 6px;
      margin-top: 10px;
    }

    .hud-pill {
      font-size: 10px;
      text-transform: uppercase;
      letter-spacing: 0.16em;
      padding: 4px 8px;
      border-radius: 999px;
      border: 1px solid var(--border-soft);
      color: var(--text-muted);
      background: rgba(5, 8, 12, 0.9);
    }

    .hud-pill--hot {
      border-color: var(--neon);
      color: var(--neon);
      background: rgba(53, 255, 122, 0.08);
      box-shadow: 0 0 12px rgba(53, 255, 122, 0.4);
    }

    .hud-cta-row {
      display: flex;
      flex-wrap: wrap;
      gap: 10px;
      margin-top: 14px;
    }

    .hud-btn {
      font-size: 11px;
      text-transform: uppercase;
      letter-spacing: 0.18em;
      padding: 8px 14px;
      border-radius: 999px;
      border: 1px solid var(--neon);
      background: radial-gradient(circle at top, rgba(53, 255, 122, 0.3), rgba(5, 8, 12, 0.95));
      color: var(--text-main);
      cursor: pointer;
      display: inline-flex;
      align-items: center;
      gap: 8px;
      box-shadow: var(--shadow-neon);
      transition: transform var(--transition-fast), box-shadow var(--transition-fast), border-color var(--transition-fast), background var(--transition-fast);
    }

    .hud-btn:hover {
      transform: translateY(-1px);
      box-shadow: 0 0 26px rgba(53, 255, 122, 0.8);
      border-color: #7bffc0;
      background: radial-gradient(circle at top, rgba(53, 255, 122, 0.45), rgba(5, 8, 12, 0.95));
    }

    .hud-btn-secondary {
      border-color: var(--border-soft);
      background: rgba(5, 8, 12, 0.9);
      box-shadow: none;
      color: var(--text-muted);
    }

    .hud-btn-secondary:hover {
      border-color: var(--neon-soft);
      color: var(--neon);
      box-shadow: 0 0 16px rgba(53, 255, 122, 0.5);
    }

    .hud-btn-dot {
      width: 7px;
      height: 7px;
      border-radius: 50%;
      background: var(--neon);
      box-shadow: 0 0 10px var(--neon);
    }

    /* ====== LIVE WIDGET MOCK (HOME EXAMPLE) ====== */

    .live-widget {
      border-radius: var(--radius);
      border: 1px solid var(--border-soft);
      background: radial-gradient(circle at top, rgba(53, 255, 122, 0.16), #050608 60%);
      padding: 10px 10px 12px;
      position: relative;
      overflow: hidden;
    }

    .live-widget-header {
      display: flex;
      align-items: center;
      justify-content: space-between;
      margin-bottom: 8px;
      font-size: 10px;
      text-transform: uppercase;
      letter-spacing: 0.18em;
      color: var(--text-muted);
    }

    .live-status {
      display: inline-flex;
      align-items: center;
      gap: 6px;
      padding: 3px 8px;
      border-radius: 999px;
      border: 1px solid rgba(255, 59, 79, 0.6);
      background: rgba(255, 59, 79, 0.12);
      color: var(--danger);
      font-size: 9px;
      letter-spacing: 0.18em;
    }

    .live-status-dot {
      width: 7px;
      height: 7px;
      border-radius: 50%;
      background: var(--danger);
      box-shadow: 0 0 10px rgba(255, 59, 79, 0.9);
    }

    .live-widget-body {
      border-radius: 6px;
      border: 1px solid var(--border-soft);
      background: radial-gradient(circle at center, rgba(0, 0, 0, 0.7), #020308 80%);
      height: 220px;
      position: relative;
      overflow: hidden;
      display: flex;
      align-items: center;
      justify-content: center;
      color: var(--text-muted);
      font-size: 11px;
      text-align: center;
      padding: 10px;
    }

    .live-widget-overlay-labels {
      position: absolute;
      inset: 8px;
      display: flex;
      flex-direction: column;
      justify-content: space-between;
      pointer-events: none;
      font-size: 9px;
      text-transform: uppercase;
      letter-spacing: 0.16em;
      color: var(--text-muted);
    }

    .live-widget-top-row,
    .live-widget-bottom-row {
      display: flex;
      justify-content: space-between;
    }

    .live-widget-label-pill {
      padding: 3px 6px;
      border-radius: 999px;
      border: 1px solid var(--border-soft);
      background: rgba(5, 8, 12, 0.9);
    }

    .live-widget-center-title {
      position: absolute;
      top: 50%;
      left: 50%;
      transform: translate(-50%, -50%);
      font-size: 13px;
      letter-spacing: 0.22em;
      text-transform: uppercase;
      color: var(--neon);
      text-shadow: 0 0 18px rgba(53, 255, 122, 0.9);
      pointer-events: none;
    }

    /* ====== FOOTER ====== */

    .hud-footer {
      width: 100%;
      max-width: 1200px;
      margin: 0 auto 10px;
      padding: 0 18px;
      font-size: 10px;
      color: var(--text-muted);
      display: flex;
      justify-content: space-between;
      gap: 10px;
      letter-spacing: 0.16em;
      text-transform: uppercase;
    }

    @media (max-width: 700px) {
      .hud-footer {
        flex-direction: column;
        align-items: flex-start;
      }
    }
  </style>
</head>
<body>
  <div class="hud-shell" id="hudShell">
    <!-- Slide-in menu backdrop -->
    <div class="hud-menu-backdrop" id="hudMenuBackdrop"></div>

    <!-- Slide-in menu trigger (skull-style icon placeholder) -->
    <button class="hud-menu-toggle" id="hudMenuToggle" aria-label="Open navigation">
      <div class="hud-menu-toggle-icon">
        <div class="hud-menu-toggle-eyes">
          <span></span><span></span>
        </div>
      </div>
    </button>

    <!-- Slide-in menu panel -->
    <aside class="hud-menu" aria-label="Main navigation">
      <header class="hud-menu-header">
        <div class="hud-menu-logo">
          DROPS<br />BY BK3
        </div>
        <div class="hud-menu-title">
          <span class="hud-menu-title-main">Tactical HUD</span>
          <span class="hud-menu-title-sub">Built for killstreaks &amp; clout</span>
        </div>
      </header>

      <div class="hud-menu-section-label">Navigation</div>
      <nav class="hud-menu-nav">
        <!-- Mark the current page with .active -->
        <a href="index.html" class="hud-menu-link active">
          <span>Home / Hub</span>
          <span class="hud-menu-link-indicator"></span>
        </a>
        <a href="drops.html" class="hud-menu-link">
          <span>Drops · Merch</span>
          <span class="hud-menu-link-indicator"></span>
        </a>
        <a href="live.html" class="hud-menu-link">
          <span>Live · Stream</span>
          <span class="hud-menu-link-indicator"></span>
        </a>
        <a href="ops.html" class="hud-menu-link">
          <span>Ops · Competitive</span>
          <span class="hud-menu-link-indicator"></span>
        </a>
        <a href="queue.html" class="hud-menu-link">
          <span>Queue · Intel</span>
          <span class="hud-menu-link-indicator"></span>
        </a>
        <a href="contact.html" class="hud-menu-link">
          <span>Contact · Support</span>
          <span class="hud-menu-link-indicator"></span>
        </a>
      </nav>

      <div class="hud-menu-section-label">Commerce</div>
      <nav class="hud-menu-nav">
        <a href="bundles.html" class="hud-menu-link">
          <span>View Bundles</span>
          <span class="hud-menu-link-indicator"></span>
        </a>
        <a href="auction.html" class="hud-menu-link">
          <span>Go to Live Shop</span>
          <span class="hud-menu-link-indicator"></span>
        </a>
      </nav>

      <footer class="hud-menu-footer">
        <span>Drops By BK3 · Tactical &amp; Gaming</span>
        <span>San Antonio, TX · Q1 · 2026</span>
      </footer>
    </aside>

    <!-- MAIN CONTENT AREA (HOME / HUB EXAMPLE) -->
    <main class="hud-main">
      <div class="hud-main-grid">
        <!-- LEFT COLUMN: Hero + Ops Intel -->
        <section class="hud-card">
          <div class="hud-card-header">
            <div class="hud-card-title">Tactical Drop Control</div>
            <div class="hud-card-tag">Live Inventory</div>
          </div>
          <div class="hud-card-body">
            <h1 class="hud-heading-main">Drops By BK3</h1>
            <p class="hud-heading-sub">
              Built like a HUD. Stocked like a boutique. Every drop feels like a loadout screen — except the gear is real.
            </p>

            <div class="hud-pill-row">
              <div class="hud-pill hud-pill--hot">Q1 · Drop 03</div>
              <div class="hud-pill">Tactical &amp; Gaming</div>
              <div class="hud-pill">Multi‑Stream Ready</div>
              <div class="hud-pill">San Antonio · TX</div>
            </div>

            <div class="hud-cta-row">
              <a href="drops.html" class="hud-btn">
                <span class="hud-btn-dot"></span>
                <span>Enter Drops</span>
              </a>
              <a href="live.html" class="hud-btn hud-btn-secondary">
                <span>View Live Widget</span>
              </a>
            </div>
          </div>
        </section>

        <!-- RIGHT COLUMN: Live Widget Preview -->
        <section class="live-widget">
          <div class="live-widget-header">
            <span>Stream Ops · Widget Preview</span>
            <span class="live-status">
              <span class="live-status-dot"></span>
              Live
            </span>
          </div>
          <div class="live-widget-body">
            <!-- This is where you’ll embed your real stream (Twitch, TikTok, etc.) on live.html -->
            <div class="live-widget-overlay-labels">
              <div class="live-widget-top-row">
                <div class="live-widget-label-pill">New Subscriber</div>
                <div class="live-widget-label-pill">New Donation</div>
              </div>
              <div class="live-widget-bottom-row">
                <div class="live-widget-label-pill">Top Kills</div>
                <div class="live-widget-label-pill">Follow for Drops</div>
              </div>
            </div>
            <div class="live-widget-center-title">Drops By BK3</div>
            <span>
              Live gameplay window / reruns preview.<br />
              On <strong>live.html</strong> this becomes your real embedded stream.
            </span>
          </div>
        </section>
      </div>

      <!-- SECOND ROW: Ops Intel + Queue Preview -->
      